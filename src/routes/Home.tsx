import React from 'react';
import styled from 'styled-components';

const MainWrap = styled.div`
  min-height: 100vh;
  background-color: yellow;
`;

function Home() {
  return <MainWrap>home</MainWrap>;
}

export default Home;
